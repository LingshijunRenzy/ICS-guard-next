# ICS-Guard 新项目开发路线图

> 目标：基于现有需求与旧版资料，从零开始重写 ICS-Guard，建立统一开发环境、清晰模块边界和可持续扩展的工程化基础。

## 1. 目标与边界

### 1.1 本阶段目标
- 从零开始搭建新代码库和新工程骨架，不继承旧版实现。
- 统一开发环境，降低新成员接入成本。
- 保持后端、控制层、前端、推理服务、设备模拟的独立演进能力。
- 提供可编排的工控设备与流量模拟能力，支撑演示、测试和模型训练场景。
- 为后续集群化、水平扩展和多环境部署预留接口。
- 将需求、架构和实现分层，避免把旧版耦合带入新项目。

### 1.2 不在本阶段直接完成的内容
- 不复用 legacy 目录中的旧代码作为主实现来源。
- 不追求一次性重构成完整微服务平台。
- 不强制引入过重的分布式中间件到最小开发闭环中。
- 不把所有协议支持和行业场景一次性铺满。

### 1.3 legacy 的使用方式
- legacy 只作为需求、接口、架构和算法思路的参考资料。
- legacy 中的实现细节、目录组织、运行方式不作为新项目默认方案。
- 如果旧版实现与新目标冲突，以新架构和新文档为准。

## 2. 推荐的总体策略

### 2.1 开发环境统一
建议使用容器化作为统一开发环境的基础：
- 后端应用使用独立容器。
- SDN 控制层使用独立容器或受限虚拟环境。
- 设备模拟使用独立容器，负责工控设备仿真和流量生成。
- 前端使用独立容器，仅负责构建和开发预览。
- 模型推理服务、数据库、缓存、消息队列按需拆分为独立服务。

这样做的直接收益是：
- 新环境只需要拉取镜像和启动编排文件。
- 依赖版本固定，减少“本地能跑、别人不能跑”的问题。
- 便于后续做测试环境、预发布环境和生产环境一致化。

### 2.2 扩展优先的架构原则
项目已经明确要求支持从单节点到集群的演进，因此新项目开发时要默认以下原则：
- 服务尽量无状态，状态外置到数据库、对象存储或消息队列。
- 配置通过环境变量或配置文件注入，禁止硬编码地址和密钥。
- API 之间使用稳定契约，避免服务内部实现影响外部调用。
- 长连接、事件流、推理任务、策略执行尽量解耦。
- 任何新增功能都要考虑后续是否可独立伸缩。

### 2.3 从头写的实现原则
- 不从 legacy 复制粘贴业务代码到新实现。
- 只复用经过确认的需求、字段定义、协议约束和测试思路。
- 先定义接口和数据契约，再实现功能。
- 先做可替换模块，再做具体实现。

## 3. 核心容器架构与服务拆分（微服务解耦）

为满足项目报告中提出的“基础设施层、控制层、应用层解耦设计”及“10Gbps线速处理、<15ms推理延迟”的苛刻性能指标，容器划分按严格的微服务边界进行，并明确以下技术栈：

- **SDN 控制层容器 (Controller)**：
    - **技术栈**：`Python 3` + `Ryu SDN Framework` + `OpenFlow 1.3`
    - **核心职责**：维持与底层交换机（或 Mininet 环境）的 OpenFlow 长连接，负责流量调度、数据面信息采集和动态下发隔离流表。对外仅暴露专属管控端口（如 6633/6653）。
- **设备模拟容器 (Device-Simulator)**：
    - **技术栈**：`Python 3` + `pymodbus` + `scapy` + `confluent-kafka`
    - **核心职责**：为演示环境和开发测试提供工控设备仿真与流量生成能力。支持可编排的工业设备拓扑（PLC、RTU、HMI、传感器、SCADA 等），自动生成 Modbus/TCP、UDP 遥测、HTTP 等正常工业背景流量，并可注入 SYN Flood、Modbus Flood、Port Scan 等攻击流量用于安全演示。产生的流量直接写入 Kafka（`ics.traffic.raw`、`ics.traffic.features` 等 Topic），或通过 Mininet 虚拟网络发送至 SDN 控制器管理的 OpenFlow 交换机端口，实现全链路集成测试。
    - **运行模式**：
        - **纯流量注入模式**（默认）：绕过 SDN 控制器，直接向 Kafka 写入模拟流量特征，适合纯后端+AI 推理的功能验证。
        - **Mininet 集成模式**（需特权容器）：在容器内运行 Mininet 虚拟拓扑，将流量通过 Open vSwitch 发送至 SDN 控制器，适合完整的 "采集→检测→响应" 闭环演示。
    - **场景编排**：通过 YAML/JSON 配置文件定义设备列表、拓扑连接、正常流量模式、攻击注入时机和类型，支持多套预置场景（小型车间、中型工厂、电网变电站）一键切换。
- **AI 推理引擎容器 (Inference-Engine)**：
    - **技术栈**：`Python 3` + `ONNX Runtime / TensorRT` + `LightGBM/DBSCAN` + `Kafka-Client (e.g. confluent-kafka)`
    - **核心职责**：通过消费离散流量特征来执行三层级联异常检测（极速过滤、核心检测、聚类确认）。为保证 `<15ms` 极低延迟，必须支持 GPU Passthrough；此类容器支持完全无状态的水平弹性扩缩容。
- **应用管控服务端容器 (App-Backend)**：
    - **技术栈**：`Java 17/21` + `Spring Boot 3` + `Spring Security` + `MyBatis-Plus / Spring Data JPA`
    - **核心职责**：提供 RESTful/GraphQL API，处理网络拓扑可视化、安全策略配置、威胁告警管理、RBAC 角色等业务逻辑层请求。
- **前端静态承载容器 (Web-UI)**：
    - **技术栈（构建期）**：`Node.js` + `Vue 3 (Composition API)` + `Vite` + `TypeScript` + `Pinia`
    - **技术栈（运行期）**：`Nginx` (Alpine极简镜像)
    - **核心职责**：为用户提供多维数据可视化界面（开发阶段兼顾前端热重载环境）。
- **消息与数据总线集群 (Data-Infrastructure)**：
    - **消息队列 (Message Bus)**：建议使用 `Redpanda` (兼容 Kafka 的 C++ 重写版，更低延迟) 或 `Apache Kafka`。用于 SDN 探针异步向 AI 引擎高并发传输流量特征信号，应对 10Gbps 流量洪峰。
    - **关系型数据库 (RDBMS)**：`PostgreSQL`，存储配置、安全策略、资产列表和告警信息等持久化状态数据。
    - **内存数据库 (Cache/KV)**：`Redis`，处理 Web 会话状态、限流防护，及资产风险评估的临时快计算。

### 3.1 容器边界隔离底线
- 绝不允许一个容器承载前端、后端、推理、数据库多个职责。
- 避免把控制器地址、模型路径、密钥硬编码进镜像，坚持环境变量注入。
- 避免在容器里写入临时状态却没有配置卷挂载（Volumes）。
- 后端容器不直接依赖本机 Java/Python 环境，必须内置 JVM 隔离运行；前端容器只负责编译/热更。

### 3.2 极限延迟下的物理与硬件配置要求（保障 <15ms 延迟，10Gbps 线速）
为了满足项目报告的苛刻指标，底层基础设施和容器挂载方式必须遵循以下物理架构原则：

#### a. 边缘计算节点（含 SDN Controller & AI Inference 容器）
**位置**：必须物理靠近核心工控交换机，最小化光纤/网线传输物理延迟。
- **网卡配置 (NIC)**：必须采用提供万兆（10Gbps）吞吐能力的网卡（如 Intel X710/E810 系列或 Mellanox 网卡）。不使用传统的 Docker NAT 网络桥接，流量分析口必须配置为 **DPDK（数据平面开发套件）** 或 **SR-IOV（单根I/O虚拟化）** 直接挂载透传进容器，绕过 Linux 内核网络栈，消灭内核上下文切换带来的延迟。
- **GPU 直通 (AI 算力)**：装配高算力/低延迟边缘显卡（如 NVIDIA T4 / A10）。利用 `NVIDIA Container Toolkit` 将 GPU 设备完全透传给 **Inference-Engine** 容器，借助 TensorRT/ONNX 实现 `PCA过滤 <1ms` 和 `轻量级判断 1-5ms` 的严苛要求。
- **CPU与内存机制**：为 **SDN Controller** 和 **AI 推理引擎** 配置 CPU 绑核（CPU Pinning），独占 CPU 物理核心；同时必须在 BIOS 级和操作系统级开启大页内存（HugePages），并严格遵循 NUMA 架构分配进程，避免跨 NUMA 内存调度造成的微秒级毛刺。

#### b. 管理与数据中枢节点（App-Backend, Web-UI, Data-Infrastructure 容器）
**位置**：可部署在企业中心机房或靠近控制节点的内网数据中心。
- **高并发数据总线（Kafka/Redpanda）**：物理存储必须使用高速 NVMe SSD 阵列（如 RAID 10），结合大容量 RDIMM 内存（128GB+），因为极速数据总线大量依赖 Linux 操作系统的 Page Cache 来缓存 10Gbps 瞬时流量，从而避免落盘带来的阻塞。
- **内存数据库与状态计算（Redis/PostgreSQL）**：内存容量需充足，因为 DBSCAN 历史基线或短时高频并发的数据需要常驻 Redis 内存并配合持久化。

### 3.3 容器间的核心通信机制（异步数据流 vs 同步控制流）

为了实现超低延迟并满足”控制层、基础设施层、应用层解耦”的目标，各个容器组件间的通信方式严格遵循 **”数据面上报走 Kafka，控制面下发走 HTTP”** 的架构原则：

#### 1. 走 Kafka 的部分（高速单向数据流）
为了应对工业网络 10Gbps 的极高并发数据冲刷，所有特征和告警数据必须由 Kafka 缓冲，防止各微服务被流量吞没。
   - **流量特征提取**：`SDN Controller` (Producer) ➡️ `Kafka Topic: ics.traffic.features` ➡️ `AI Inference-Engine` (Consumer)
     - **业务场景**：SDN 探针抓取底层镜像流量提炼特征后，全速异步打入队列。AI 引擎拉取该队列执行极速检测。
   - **威胁告警触发**：`AI Inference-Engine` (Producer) ➡️ `Kafka Topic: ics.threat.alerts` ➡️ `App-Backend` (Consumer)
     - **业务场景**：AI 判定发生威胁后不等待任何结果，直接将告警事件推入队列。Java 后端异步消费入库 (PostgreSQL) 供前端查询。
   - **模拟流量注入**（仅开发/演示环境）：`Device-Simulator` (Producer) ➡️ `Kafka Topic: ics.traffic.raw | ics.traffic.features` ➡️ `AI Inference-Engine` / `App-Backend` (Consumer)
     - **业务场景**：设备模拟器按编排场景生成工业协议流量（Modbus 读写、UDP 遥测、HTTP 巡检），直接写入 Kafka，供下游消费者验证检测引擎和告警链路，无需依赖真实物理网络或 Mininet 环境。

#### 2. 走 HTTP 的部分（同步双向控制流）
控制指令需要“确定的执行结果”（成功/失败），且频次远低于报文特征提取，必须使用 HTTP(RESTful) 同步调用。
   - **精准阻断下发**：`App-Backend` ➡️ `SDN Controller`
     - **业务场景**：Java 后端根据安全策略（或前端运维人员点击），调用 SDN 控制器极简接口（如 `POST /api/v1/flow/block`），要求控制器向交换机下发物理封堵流表。
   - **前后端界面交互**：`Web-UI` ↔️ `App-Backend`
     - **业务场景**：标准的 Web 交互，前端获报表、拓扑图、告警列表等视图数据，下发系统配置。

## 4. 基于报告场景的编排演进策略

项目报告明确指出了三种典型部署规模，新项目的容器编排需按此路径动态演进：

### 4.1 初创开发与小型车间场景（单节点，Docker Compose）
- **特征**：所有微服务运行在单台边缘计算服务器上。
- **网络与存储**：使用桥接网络（Bridge）通信，数据库使用本地 Host 挂载卷。
- **实施路径**：开发期直接在一份 `docker-compose.yml` 中启动前端、后端、设备模拟器、AI 推理引擎、消息总线、数据库，实现”一键启动全套基础服务”的开发环境闭环。其中设备模拟器默认以纯流量注入模式运行，不依赖 Mininet 特权容器。
- **适用性**：前期研发验证，及算力要求可被单节点承载的小型工厂/车间。

### 4.2 中型工厂场景（主从架构，K3s / Docker Swarm）
- **特征**：拆分管理节点与计算节点。
- **调度策略**：
    - **主节点（Master）**：部署 App-Backend、Web-UI、PostgreSQL 等重逻辑、轻算力、多状态的容器。
    - **从节点（Worker）**：下沉部署到靠近核心交换机的场站设备，专跑 SDN Controller 和 AI Inference-Engine，充分利用宿主机网卡直通和 GPU 性能，执行流量抓取与本地分析。

### 4.3 大型集团场景（分布式集群，生产级 Kubernetes）
- **特征**：标准 Kubernetes 集群 + Helm 统一部署，跨区高可用。
- **高级编排特性（针对安全业务需求）**：
    - **HPA（自动水平扩缩容）**：AI 检测节点根据 CPU/GPU 负载或 Kafka 积压量自动横向扩容，多实例并发吃下日均 1.2TB 乃至 10Gbps 的数据洪峰。
    - **节点亲和性（Affinity）**：强制 SDN Controller 与 AI 引擎进行网络亲和部署，减少跨物理机网络传输延迟，保障“自动化隔离响应时间小于 1 秒”。
    - **零停机热更（Rolling Updates）**：应对工控网络的高业务连续性要求，在升级检测模型或新增安全节点时实现系统不停机。

## 5. 开发阶段规划

### Phase 1: 统一开发环境
目标是“本地一条命令能启动全套基础服务”。

交付物：
- 新项目脚手架与目录结构。
- 后端容器镜像。
- 控制层容器镜像。
- 设备模拟容器镜像（含预置演示场景）。
- 前端容器镜像。
- `docker compose` 开发编排文件。
- 统一配置样例文件。

验收标准：
- 新开发者可在最短时间内启动项目。
- 服务间地址通过环境变量配置，不依赖手工改代码。
- 基础健康检查可用。

### Phase 2: 模块拆分与接口稳定
目标是“服务职责清晰，彼此通过稳定接口协作”。

交付物：
- 推理服务独立化。
- 协议解析逻辑模块化。
- 控制层调用接口稳定化。
- 前后端 API 契约固定化。
- 旧版接口映射表和新接口设计说明。

验收标准：
- 推理逻辑可单独启动和测试。
- 控制层故障不影响前端基础浏览。
- 关键接口有版本管理。

### Phase 3: 事件驱动与消息总线
目标是“把高频异步流量从同步调用中剥离”。

交付物：
- 消息总线接入。
- 流量、告警、审计、模型事件分 topic 传递。
- 策略执行与响应动作异步化。

验收标准：
- 事件消费可以独立扩容。
- 高峰流量不会把主业务 API 拖死。

### Phase 4: 集群化与高可用
目标是“从能跑变成能长期稳定运行”。

交付物：
- 多副本部署方案。
- 负载均衡和健康检查。
- 资源限制和自动扩缩容策略。
- 数据备份与恢复流程。

验收标准：
- 单个服务实例挂掉后可自动恢复。
- 新实例能自动加入服务池。
- 升级过程支持尽量不停机。

## 6. 目录与责任建议

目录结构（已创建，各团队在各自目录内自行组织源码）：

| 目录 | 归属 | 说明 |
|------|------|------|
| `app-backend/` | Team A | Java Spring Boot 应用层 |
| `frontend/` | Team A | Vue 3 前端工程 |
| `sdn-controller/` | Team B | Python Ryu SDN 控制层 |
| `inference-engine/` | Team B | AI 推理引擎 |
| `simulator/` | Team B | 设备模拟与流量生成（场景配置、设备模型、攻击剧本） |
| `models/` | Team B | 模型文件与阈值配置 |
| `deploy/` | 共享 | 容器编排 (`docker-compose.yml` 在仓库根目录)、K8s Helm Chart、环境配置 |
| `docs/` | 共享 | 架构设计、接口契约、协议说明、安全文档 |

如需保留 legacy，仅放在独立参考目录，不参与新工程构建链路。

## 7. 需要优先确认的工程问题

开发前最好先确认以下事项：
- 新仓库是否完全独立于 legacy，还是只沿用文档和数据资产。
- 数据库选型是继续使用 SQLite 还是切换 PostgreSQL。
- 是否引入 Redis 作为缓存和会话辅助层。
- 是否选择 Kafka、Redpanda 或 NATS 作为消息总线。
- 是否计划把推理服务独立为独立进程或独立容器。
- 前端开发是否使用独立容器，还是继续本机热更新。
- 设备模拟器的默认运行模式是纯流量注入还是 Mininet 集成。
- 演示场景需要覆盖哪些工业协议和攻击类型（Modbus/104/DNP3，DoS/扫描/MITM 等）。

## 8. 建议的最小执行清单

1. 先新建项目骨架和目录结构。
2. 再补齐开发用容器编排文件（含设备模拟容器）。
3. 接着把配置、密钥、模型路径外部化。
4. 然后定义接口、数据契约和最小闭环流程。
5. 最后引入消息通道、可观测性和集群化能力。

## 9. 结论

这个项目适合走“从零开始重写 + 容器化统一开发 + 模块化拆分 + 分阶段扩展”的路线。原因很直接：
- legacy 只适合做需求和设计参考，不适合直接继承为新实现。
- 项目对扩展性、部署一致性和长期维护要求很高。
- 容器化能把开发环境、测试环境和后续交付环境拉到同一条线上。

如果后续要正式开发，建议把“新项目脚手架 + 容器化 + 接口契约”作为第一优先级基础工程，而不是后补项。